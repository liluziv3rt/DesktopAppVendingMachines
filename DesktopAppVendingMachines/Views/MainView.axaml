<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:DesktopAppVendingMachines.ViewModels"
             xmlns:views="using:DesktopAppVendingMachines.Views"
             xmlns:converters="using:DesktopAppVendingMachines.Converters"
             mc:Ignorable="d" d:DesignWidth="1200" d:DesignHeight="800"
             x:Class="DesktopAppVendingMachines.Views.MainView"
             x:DataType="vm:MainViewModel">

    <Grid RowDefinitions="70, 50, *" ColumnDefinitions="280, *">
        
        <!-- Верхний бар (белый) с логотипом и пользователем -->
        <Border Grid.Row="0" Grid.ColumnSpan="2" 
                Background="White" 
                BoxShadow="0 2 8 #1A000000"
                Padding="20 0">
            <Grid ColumnDefinitions="Auto, *, Auto">
                
                <!-- Логотип слева -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                    <Image Source="/Assets/Logo.png" 
                                   Width="40" Height="40" 
                                   Stretch="UniformToFill"/>
                    </StackPanel>
                
                <!-- Пустое место по центру -->
                <TextBlock Grid.Column="1" Text=""/>
                
                <!-- Блок пользователя справа -->
                <Border Grid.Column="2" 
                        Background="#F5F7FA" 
                        CornerRadius="30" 
                        Padding="10 5"
                        VerticalAlignment="Center">
                    <Grid ColumnDefinitions="Auto, Auto" ColumnSpacing="10">
                        <Border Grid.Column="0" 
                                Width="40" Height="40" 
                                CornerRadius="20" 
                                Background="#3498DB"
                                ClipToBounds="True">
                            <Image Source="/Assets/Flag.png" 
                                   Width="40" Height="40" 
                                   Stretch="UniformToFill"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding UserName}" 
                                       Foreground="#2C3E50" 
                                       FontSize="14" 
                                       FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding UserRole}" 
                                       Foreground="#7F8C8D" 
                                       FontSize="12"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Border>
        
        <!-- Левое меню (темное) -->
        <Border Grid.Row="1" Grid.RowSpan="2" Grid.Column="0" Background="#1E2A3A">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="25" Spacing="20">
                    
                    <!-- Заголовок Навигация -->
                    <TextBlock Text="Навигация" 
                               FontSize="14" 
                               FontWeight="SemiBold"
                               Foreground="#BDC3C7" 
                               Margin="0 10 0 0"/>

                    <!-- Пункты меню -->
                    <StackPanel Spacing="8">
                        <!-- Главная -->
                        <Button Content="🏠 Главная" 
                                Command="{Binding NavigateToCommand}"
                                CommandParameter="Main"
                                Background="{Binding CurrentPageTitle, Converter={StaticResource PageToColorConverter}, ConverterParameter=Главная}"
                                Foreground="White" 
                                HorizontalAlignment="Stretch" 
                                HorizontalContentAlignment="Left" 
                                Padding="20 12" 
                                CornerRadius="8" 
                                BorderThickness="0"/>
                        
                        <!-- Монитор ТА -->
                        <Button Content="📊 Монитор ТА" 
                                Command="{Binding NavigateToCommand}"
                                CommandParameter="Monitor"
                                Background="{Binding CurrentPageTitle, Converter={StaticResource PageToColorConverter}, ConverterParameter=Монитор ТА}"
                                Foreground="White" 
                                HorizontalAlignment="Stretch" 
                                HorizontalContentAlignment="Left" 
                                Padding="20 12" 
                                CornerRadius="8" 
                                BorderThickness="0"/>
                        
                        <!-- Детальные отчеты -->
                        <Button Content="📈 Детальные отчеты" 
                                Command="{Binding NavigateToCommand}"
                                CommandParameter="Reports"
                                Background="{Binding CurrentPageTitle, Converter={StaticResource PageToColorConverter}, ConverterParameter=Детальные отчеты}"
                                Foreground="White" 
                                HorizontalAlignment="Stretch" 
                                HorizontalContentAlignment="Left" 
                                Padding="20 12" 
                                CornerRadius="8" 
                                BorderThickness="0"/>
                        
                        <!-- Учет ТМЦ -->
                        <Button Content="📦 Учет ТМЦ" 
                                Command="{Binding NavigateToCommand}"
                                CommandParameter="Inventory"
                                Background="{Binding CurrentPageTitle, Converter={StaticResource PageToColorConverter}, ConverterParameter=Учет ТМЦ}"
                                Foreground="White" 
                                HorizontalAlignment="Stretch" 
                                HorizontalContentAlignment="Left" 
                                Padding="20 12" 
                                CornerRadius="8" 
                                BorderThickness="0"/>
                        
                        <!-- Администрирование с подпунктами -->
                        <Expander Header="⚙️ Администрирование" 
                                  Foreground="#BDC3C7"
                                  Background="Transparent"
                                  BorderThickness="0"
                                  IsExpanded="{Binding IsAdminMenuExpanded}">
                            <StackPanel Margin="20 10 0 0" Spacing="5">
                                
                                <!-- Торговые автоматы -->
                                <Button Content="📋 Торговые автоматы" 
                                        Command="{Binding NavigateToCommand}"
                                        CommandParameter="VendingMachines"
                                        Background="{Binding CurrentPageTitle, Converter={StaticResource PageToColorConverter}, ConverterParameter=Торговые автоматы}"
                                        Foreground="White" 
                                        HorizontalAlignment="Stretch" 
                                        HorizontalContentAlignment="Left" 
                                        Padding="15 10" 
                                        CornerRadius="8" 
                                        BorderThickness="0"
                                        FontSize="14"/>
                                
                                <!-- Компании -->
                                <Button Content="🏢 Компании" 
                                        Command="{Binding NavigateToCommand}"
                                        CommandParameter="Companies"
                                        Background="{Binding CurrentPageTitle, Converter={StaticResource PageToColorConverter}, ConverterParameter=Компании}"
                                        Foreground="White" 
                                        HorizontalAlignment="Stretch" 
                                        HorizontalContentAlignment="Left" 
                                        Padding="15 10" 
                                        CornerRadius="8" 
                                        BorderThickness="0"
                                        FontSize="14"/>
                                
                                <!-- Пользователи -->
                                <Button Content="👥 Пользователи" 
                                        Command="{Binding NavigateToCommand}"
                                        CommandParameter="Users"
                                        Background="{Binding CurrentPageTitle, Converter={StaticResource PageToColorConverter}, ConverterParameter=Пользователи}"
                                        Foreground="White" 
                                        HorizontalAlignment="Stretch" 
                                        HorizontalContentAlignment="Left" 
                                        Padding="15 10" 
                                        CornerRadius="8" 
                                        BorderThickness="0"
                                        FontSize="14"/>
                                
                                <!-- Модемы -->
                                <Button Content="📡 Модемы" 
                                        Command="{Binding NavigateToCommand}"
                                        CommandParameter="Modems"
                                        Background="{Binding CurrentPageTitle, Converter={StaticResource PageToColorConverter}, ConverterParameter=Модемы}"
                                        Foreground="White" 
                                        HorizontalAlignment="Stretch" 
                                        HorizontalContentAlignment="Left" 
                                        Padding="15 10" 
                                        CornerRadius="8" 
                                        BorderThickness="0"
                                        FontSize="14"/>
                                
                                <!-- Дополнительные -->
                                <Button Content="➕ Дополнительные" 
                                        Command="{Binding NavigateToCommand}"
                                        CommandParameter="Additional"
                                        Background="{Binding CurrentPageTitle, Converter={StaticResource PageToColorConverter}, ConverterParameter=Дополнительные}"
                                        Foreground="White" 
                                        HorizontalAlignment="Stretch" 
                                        HorizontalContentAlignment="Left" 
                                        Padding="15 10" 
                                        CornerRadius="8" 
                                        BorderThickness="0"
                                        FontSize="14"/>
                            </StackPanel>
                        </Expander>
                        
                        <!-- Выход -->
                        <Button Content="🚪 Выход" 
                                Command="{Binding LogoutCommand}"
                                Background="Transparent" 
                                Foreground="#E74C3C" 
                                HorizontalAlignment="Stretch" 
                                HorizontalContentAlignment="Left" 
                                Padding="20 12" 
                                CornerRadius="8" 
                                BorderThickness="0" 
                                Margin="0 20 0 0"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Второй бар (темно-синий) с названием компании и страницы -->
        <Border Grid.Row="1" Grid.Column="1" Background="#2C3E50" Padding="20 0">
            <Grid ColumnDefinitions="Auto, Auto, *">
                <TextBlock Text="ООО Торговые Автоматы" 
                           FontSize="16" 
                           FontWeight="SemiBold"
                           Foreground="White" 
                           VerticalAlignment="Center"/>
                
                <TextBlock Grid.Column="1" 
                           Text="|" 
                           FontSize="16"
                           Foreground="#BDC3C7" 
                           Margin="15 0"
                           VerticalAlignment="Center"/>
                
                <TextBlock Grid.Column="2" 
                           Text="{Binding CurrentPageTitle}" 
                           FontSize="16"
                           Foreground="#BDC3C7" 
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Основная область с динамической загрузкой страниц -->
        <Grid Grid.Row="2" Grid.Column="1" Background="#F5F7FA">
            <!-- Если CurrentPage = null, показываем MainViewContent -->
            <views:MainViewContent IsVisible="{Binding CurrentPage, Converter={StaticResource IsNull}}"/>
            
            <!-- Если CurrentPage не null, показываем его -->
            <ContentControl Content="{Binding CurrentPage}"
                            IsVisible="{Binding CurrentPage, Converter={StaticResource IsNotNull}}"/>
        </Grid>
    </Grid>
</UserControl>